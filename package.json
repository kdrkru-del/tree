{
  "name": "tree-service-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Отдельный статический многостраничный сайт услуг по спилу, обрезке деревьев и расчистке участков.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "check": "node --check src/app.js && node --check worker/lead-worker.mjs && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
